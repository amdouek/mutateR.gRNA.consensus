% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_grnas_multi.R
\name{score_grnas_multi}
\alias{score_grnas_multi}
\title{Score gRNAs using multiple on-target scoring methods}
\usage{
score_grnas_multi(
  grna_gr,
  methods = NULL,
  tracr = "Chen2013",
  deephf_var = "wt_u6",
  quiet = FALSE
)
}
\arguments{
\item{grna_gr}{GRanges returned by find_cas9_sites() or find_cas12a_sites().}

\item{methods}{Character vector of scoring methods to run.
For Cas9: any subset of c("ruleset1", "deepspcas9", "ruleset3", "deephf")
For Cas12a: any subset of c("deepcpf1", "enpamgb")
Default NULL auto-selects based on detected nuclease.}

\item{tracr}{Character. tracrRNA for RuleSet3 (default "Chen2013").}

\item{deephf_var}{Character. DeepHF variant (default "wt_u6").}

\item{quiet}{Logical. Suppress progress messages (default FALSE).}
}
\value{
A data.frame with columns:
\itemize{
\item grna_id: Unique identifier for each gRNA
\item protospacer_sequence: The protospacer sequence
\item exon_rank: Exon location
\item One column per scoring method containing raw scores
}
}
\description{
Runs multiple scoring methods on the same set of gRNAs and returns
all scores in a single data frame for comparative analysis.
}
\examples{
\dontrun{
library(BSgenome.Hsapiens.UCSC.hg38)

# Get gRNA sites
exons <- get_exon_structures("ENST00000269305", "hsapiens", output = "GRanges")
sites <- find_cas9_sites(exons, BSgenome.Hsapiens.UCSC.hg38)

# Score with multiple methods
multi_scores <- score_grnas_multi(sites,
                                  methods = c("ruleset1", "deepspcas9", "ruleset3", "deephf"))

# Explore correlations
plot_score_correlations(multi_scores)
}

}
